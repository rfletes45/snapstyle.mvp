# Colyseus Server Environment Variables

# Port Colyseus listens on
COLYSEUS_PORT=2567

# Path to Firebase service account key JSON (Method 1 — file-based)
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json

# OR paste the full JSON content of the service account key here (Method 2 — env-based)
# This avoids needing a separate file — ideal for deployment platforms (Heroku, Railway, Render).
# Get it from: Firebase Console → Project Settings → Service accounts → Generate new private key
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key":"...","client_email":"...",...}

# OR set GOOGLE_APPLICATION_CREDENTIALS to the key file path (Method 3 — ADC)
# Works automatically on GCP (Cloud Run, App Engine, etc.)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccountKey.json

# Node environment
NODE_ENV=development

# Reconnection timeouts (in seconds) per game tier
RECONNECTION_TIMEOUT_PHYSICS=30
RECONNECTION_TIMEOUT_TURNBASED=300
RECONNECTION_TIMEOUT_QUICKPLAY=15
RECONNECTION_TIMEOUT_COOP=60

# Server identity (for multi-region)
SERVER_REGION=us-east-1

# Logging level: debug | info | warn | error
LOG_LEVEL=info
